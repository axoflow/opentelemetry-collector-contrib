type: crowdstrike

status:
  class: receiver
  stability:
    development: [logs]
  distributions: [contrib]
  codeowners:
    active: [overorion]
  unsupported_platforms: []

tests:
  config:
    client_id: "test-client-id"
    client_secret: "test-client-secret"
    cloud: "us-1"
